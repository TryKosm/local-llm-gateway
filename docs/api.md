# API Draft

Planned HTTP surface:
- `POST /v1/chat` routes model request and returns selected provider metadata.
- `GET /v1/providers` lists provider health snapshots.
- `GET /health` basic service status.
